---
phase: 03-visual-refinement
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05
  - THEME-06
  - THEME-07
  - THEME-08
  - THEME-09

must_haves:
  truths:
    - "The entire app uses warm neutral tones with zero blue tint"
    - "No per-type rainbow colors are visible anywhere"
    - "Cards are solid elevated surfaces with bottom-border headers and section separators"
    - "Editor syntax is muted and professional"
    - "Toolbar is a right-docked vertical bar"
    - "All colors reference CSS custom properties -- no hardcoded hex in components"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Phase 3 visual refinement. Human confirms that all surfaces -- cards, canvas, editor, inspector, toolbar, command palette -- use the warm dark palette with no residual blue tint, no per-type rainbow colors, and professional devtool aesthetics.

Purpose: Catch visual regressions, color mismatches, or missed surfaces that automated checks cannot detect. Human eyes verify the holistic aesthetic.

Output: Confirmation that the visual refinement meets the Linear/Vercel-quality target, or a list of specific issues to address.
</objective>

<execution_context>
@/Users/thedoc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thedoc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-visual-refinement/03-01-SUMMARY.md
@.planning/phases/03-visual-refinement/03-02-SUMMARY.md
@.planning/phases/03-visual-refinement/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification of complete Phase 3 refinement</name>
  <files>N/A -- verification only</files>
  <action>
Present the completed visual refinement to the user for review. Run `npm run dev` to ensure the dev server is available. Walk through each verification step with the user.

What was built:
- Warm dark token palette via @theme CSS custom properties (Plan 01)
- Solid surface cards with section separators and neutral section dots (Plan 02)
- Warm canvas dot grid, muted MiniMap/Controls (Plan 02)
- Muted dimension edges -- desaturated default, vivid on hover (Plan 02)
- Desaturated editor syntax highlighting (Plan 03)
- Right-docked vertical toolbar (Plan 03)
- Warm inspector panel and command palette without per-type colors (Plan 03)
  </action>
  <verify>
Human verifies visually:
1. Run `npm run dev` and open `http://localhost:5173`
2. Canvas check: Dot grid warm neutral (not blue). Background near-black warm (#111).
3. Card check: Solid dark surfaces, no glass/blur. Header has bottom border only (no colored accent bar). Subtle section separator lines. Section dots use neutral shades.
4. Edge check: Dimension edges faint/muted by default. Hover brightens to full color.
5. Editor check (Cmd+E): Muted/desaturated syntax -- no bright neon colors.
6. Toolbar check: Vertical bar docked right, sharp corners, icons stacked vertically.
7. Command palette (Cmd+K): Warm tones, no rainbow type dots.
8. Inspector check: Warm tree view, no per-type rainbow colors.
9. Subgraph check: Click permission row -- warm tones persist, breadcrumb warm, transitions smooth.
10. Overall: Professional devtool aesthetic (Linear/Vercel), not AI-generated UI.
  </verify>
  <done>Human types "approved" confirming warm dark palette, no blue tint, no rainbow colors, professional aesthetic across all surfaces. Or provides specific issues to address in a gap closure plan.</done>
</task>

</tasks>

<verification>
Human visual verification covers all 9 THEME requirements holistically.
</verification>

<success_criteria>
- Human confirms warm dark palette across all surfaces
- Human confirms no blue tint anywhere
- Human confirms no per-type rainbow colors
- Human confirms professional devtool aesthetic
- Human approves or provides specific fixable feedback
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-refinement/03-04-SUMMARY.md`
</output>
